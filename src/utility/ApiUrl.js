import {BaseUrl} from './BaseUrl';

export const SendOtp = BaseUrl + '/request-otp';
export const VerifyOtp = BaseUrl + '/verify-otp';
export const GetProfile = BaseUrl + '/profile';
export const UpdateProfile = BaseUrl + '/profile-update';

